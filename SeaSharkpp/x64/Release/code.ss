using System
using OpenGL
using FileWriter

struct vector
{
    x = 0
    y = 0
    z = 0
}

position = vector(["x", 5, "y", 10, "z", 20])

printline(position.x)
printline(position.y)
printline(position.z)